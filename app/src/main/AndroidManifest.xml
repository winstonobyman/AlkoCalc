<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.alkocalc">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/alko_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreen" />
        <activity android:name=".InfoPanel" />
        <activity android:name=".DateDiary" />
        <activity android:name=".DoseDrunkResult" />
        <activity android:name=".DoseDrunk" />
        <activity android:name=".TimeSoberResult" />
        <activity android:name=".TimeSober" />
        <activity android:name=".ChangeAttrs" />
        <activity android:name=".FirstScreen" />
        <activity android:name=".ThirdScreen" />
        <activity android:name=".SecondScreen" />
        <activity android:name=".ChooseFunction">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>