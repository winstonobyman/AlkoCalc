<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textSize">25sp</item>
        <item name="fontFamily">@font/type_mates_cera_round_pro_regular</item>
    </style>

    <style name="AppTheme.TextView">
        <item name="android:textSize">30sp</item>
        <item name="android:textAlignment">center</item>
        <item name="android:inputType"> textNoSuggestions</item>
    </style>

    <style name="AppTheme.TextView.Hello">
        <item name="android:textColor">@color/colorMajorPink</item>
        <item name="fontFamily">@font/type_mates_cera_round_pro_medium</item>
    </style>

    <style name="AppTheme.ChooseIcon">
        <item name="android:layout_width">140dp</item>
        <item name="android:layout_height">140dp</item>
        <item name="android:layout_margin">32dp</item>
    </style>

    <style name="AppTheme.RadioButton">
        <item name="android:buttonTint">@color/colorMajorPink</item>
        <item name="android:textColor">@color/colorMajorPink</item>
        <item name="fontFamily">@font/type_mates_cera_round_pro_bold</item>
    </style>

    <style name="AppTheme.Button">
        <item name="android:layout_width">250dp</item>
        <item name="android:layout_height">50dp</item>
        <item name="android:background">@drawable/round_rect_shape</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
